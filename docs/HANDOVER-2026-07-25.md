# Handover — morning of Sat 25 July 2026

Written at close of Friday-night session. State of play + where to go next.

## What shipped today (all live & verified)

1. **email-inbound-bridge is LIVE end-to-end.** Root cause of last night's failure: `RESEND_API_KEY` was the old "Onboarding" key (sending-only) — couldn't read received emails → 502s. Greg swapped it for a full-access key; replayed both webhook events; both routes verified:
   - notes@venuachiax.resend.app → injector_notes → Note Master extracted (Bridget Test note)
   - leads@venuachiax.resend.app → buyer-enquiry-inbound → ack email + both SMS + Greg Bridgetest buyer card
   - Bridge error logging improved (surfaces upstream HTTP status + body).
2. **Newsletter capture pipeline LIVE:** signup blocks on funnel landing + thank-you pages, Netlify proxy (`newsletter-subscribe.js`, CORS allows gregleighproperty.com.au), `newsletter-subscribe` edge fn → CRM contact (tag `newsletter`, fill-empty-only) + Resend Audience contact + branded welcome email (BCC Greg). Tested end-to-end with Greg's email — all three legs green.
3. **The Redlands Insider issue #1** (`newsletter/issue-01.html`): short email, navy header + white QSIR logo (Outlook-safe table/bgcolor), 4 teaser cards → full articles, market pulse, off-market corner, appraisal CTA, Resend unsubscribe merge tag.
4. **News hub LIVE:** redlandshomevalue.com.au/news — index + 4 articles (land valuations 20%, Toondah reset, City Plan splittable blocks, Birkdale Olympics), each with appraisal CTA + sources. SEO-indexable.
5. **CRM → Resend export:** `newsletter-import-resend.csv` (196 clean contacts; opt-outs/DNC/test junk excluded).
6. Yesterday's ads work still standing: Champion + Challenger B running at $30/day; Challenger C drafted unpublished; day-1 ad report fires 9:30am Sat (app must be open — approve "Run now" if prompted).

## Greg's manual to-dos (in rough priority)

1. **Send issue #1:** Resend → Audience → Add contacts → import `newsletter-import-resend.csv`. Then Broadcasts → paste `newsletter/issue-01.html` → subject: "Your land value just jumped 20% — here's what it really means" → TEST SEND to yourself first → send. (Before sending: eyeball the off-market corner copy — currently generic "reply to me", fine to ship as-is.)
2. **Rewire senders to the bridge:** Pocket summaries → notes@venuachiax.resend.app; Outlook rule forwarding REA/Domain enquiries → leads@venuachiax.resend.app. Until done, buyer enquiries are NOT auto-processed.
3. Paste `lead-funnel/newsletter-embed-snippet.html` into gregleighproperty.com.au.
4. Meta account authentication (#3858385) if still pending — ads stop without it.
5. Note Inbox: 31 pending proposals to review (+ dismiss the two bridge-test notes: "BRIDGE TEST — Door knock Ormiston" and "Buyer enquiry — Greg Bridgetest").

## Claude to-dos next session

1. **Cleanup test data** once Greg's seen it: two bridge-test notes + their proposals; optionally strip `newsletter` tag from the "For Real" contact (Greg's own email — harmless).
2. **Tell work Claude** (Drive handover): Pocket sweep is retired — bridge handles ingestion now; gmail-crm-sweep scheduled task is disabled.
3. Check Challenger B day-1 numbers with Greg after the 9:30am report.

## Ideas for where to go from here (morning brainstorm fodder)

- **Schedule issue #2** for late August: research + draft ~Aug 25 so it lands 1 Sept — "Spring is here" edition. Natural lead: spring listing checklist + what August buyer data showed. Could set a Cowork scheduled task like the ad report.
- **Add a "News" link** to the funnel header/footer + thank-you page so organic visitors find the hub (2-line change).
- **Welcome email → link the news hub** so new subscribers get instant content (1-line change in newsletter-subscribe fn).
- **Off-market corner → real inventory:** pull actual buyer briefs / quiet listings from the CRM each issue. Could even auto-generate from listings table.
- **Insider archive**: news index already doubles as archive — just keep adding article pages per issue; consider grouping by issue.
- **SEO basics for /news:** sitemap.xml + og:image per article; suburb-name keywords already in copy.
- **Newsletter growth loops:** FB/IG bio links → /news; boost the best article as a cheap traffic ad ($5/day) pointing at the hub (softer entry than the appraisal LP); QR code on letterbox drops.
- **Segment later:** when list grows, tag CRM contacts buyer vs seller and use Resend segments/topics for targeted sends.
- **Measure:** watch Resend broadcast opens/clicks; whichever teaser card gets most clicks = next issue's lead-story style.

## Key refs
- Resend webhook ID 4ec9f9e4-4435-40f7-995b-df2bdb343cc8 → email-inbound-bridge (email.received, Enabled)
- Inbound address family: `<anything>@venuachiax.resend.app`
- Newsletter fn auth: Bearer INBOUND_WEBHOOK_SECRET (same as inbound-lead)
- CRM newsletter contacts: `tags @> ['newsletter']`, source `newsletter_valuation_lp` / `newsletter_glp_site` / `newsletter_signup`
